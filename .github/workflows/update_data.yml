name: Update Data from Google Sheet

on:
  # 구글 앱스 스크립트에서 보내는 신호(google_sheet_update)를 받습니다.
  repository_dispatch:
    types: [google_sheet_update]
    
  schedule:
    - cron: '0 * * * *' # 1시간마다 자동 백업/실행
  workflow_dispatch:      # 수동 실행 버튼 활성화

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  fetch-and-save:
    runs-on: ubuntu-latest
    
    # [수정됨] 이 부분이 추가되었습니다. GitHub Pages 배포를 위해 환경 설정이 필요합니다.
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          pip install requests pytz

      - name: Fetch Data
        run: |
          # 1. 구글 시트 데이터 가져오기 (data.json)
          curl -L "https://script.google.com/macros/s/AKfycbwGGJsflqUqpv4ep9NU8BV_zXpqTDzrcgGER6vKzKOeOBNIVeZFyAJdbSl3pWscSyTA/exec?action=getInitialData" -o data.json
          
          # 2. 기상청 날씨 데이터 업데이트 (weather.json)
          python update_weather.py

      - name: Commit and Push
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          
          git add data.json weather.json
          
          if ! git diff --cached --quiet; then
            git commit -m "Update data and weather via KMA API"
            git push
          else
            echo "No changes to commit"
          fi

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
